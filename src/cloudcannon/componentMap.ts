// Single source of truth mapping content-collection `_type` keys to their
// Astro block components. Phase 4 populates this as each block is authored.
// Phase 6's editable-regions verification greps this file.

export const componentMap: Record<string, unknown> = {
  // hero_block: HeroBlock,    (added in Phase 4)
  // browse_lineup: BrowseLineupBlock,
  // proadvantage: ProAdvantageBlock,
  // tv_walls: TvWallsBlock,
  // faq: FaqBlock,
  // press_strip: PressStripBlock,
  // quote: QuoteBlock,
};
